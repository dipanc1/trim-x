import { StyleSheet, Text, View } from "react-native";
import React from "react";

const Trim = () => {
  return (
    <View>
      <Text>Trim</Text>
    </View>
  );
};

export default Trim;

const styles = StyleSheet.create({});
